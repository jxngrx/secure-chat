import 'package:flutter/material.dart';

class OngoingCallScreen extends StatelessWidget {
  const OngoingCallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Ongoing Call Screen'),
      ),
    );
  }
}
